# file: python/defaults/main.yml
python_source_version: "3.8"
python_apt_ppa:
python_packages:
  - "python{{ source_version }}"
  - "python{{ source_version }}-dev"
  - "python{{ source_version.split('.')[0] }}-distutils"
  - "python{{ source_version.split('.')[0] }}-pip"
