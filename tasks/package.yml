# file: python/tasks/default.yml

- name: python | Make sure the packages are installed (Debian)
  apt:
    pkg: "{{ debian_python_packages }}"
    state: present
  when: ansible_os_family == "Debian"
